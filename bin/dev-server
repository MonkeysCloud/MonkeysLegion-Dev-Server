#!/usr/bin/env php
<?php
require __DIR__ . '/../vendor/autoload.php';

$server = new \MonkeysLegion\DevServer\DevServer();
$host   = $_SERVER['ML_DEV_HOST'] ?? '127.0.0.1';
$port   = (int) ($_SERVER['ML_DEV_PORT'] ?? 8000);
$server->serve($host, $port);
